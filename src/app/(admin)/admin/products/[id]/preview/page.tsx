import { notFound } from "next/navigation";import { getProduct } from "@/lib/data";import { Badge } from "@/components/ui/badge";import { PageHeader } from "@/components/page-header";
export default async function Preview({params}:{params:Promise<{id:string}>}){const p=await getProduct((await params).id);if(!p)notFound();return <><PageHeader title="Преглед на продукта" description="Така ще изглежда основната продуктова информация."/><article className="mx-auto max-w-3xl rounded-lg border bg-background p-6 md:p-10"><Badge>{p.status==='published'?'Публикуван':'Преглед'}</Badge><h1 className="mt-4 text-3xl font-semibold">{p.name}</h1><p className="mt-2 text-muted-foreground">{p.categories?.name}</p>{p.short_description&&<p className="mt-6 text-lg">{p.short_description}</p>}<div className="mt-6 whitespace-pre-wrap leading-7">{p.description}</div></article></>}
