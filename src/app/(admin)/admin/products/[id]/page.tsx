import { notFound } from "next/navigation";import Link from "next/link";import { PageHeader } from "@/components/page-header";import { ProductForm } from "@/components/product-form";import { Button } from "@/components/ui/button";import { getCategories,getProduct } from "@/lib/data";
export default async function Edit({params}:{params:Promise<{id:string}>}){const {id}=await params;const [product,categories]=await Promise.all([getProduct(id),getCategories()]);if(!product)notFound();return <div className="mx-auto w-full max-w-5xl"><PageHeader title={product.name} description="Редактирайте информацията, статуса и SEO полетата." action={<Button asChild variant="outline"><Link href={`/admin/products/${id}/preview`}>Преглед</Link></Button>}/><ProductForm product={product} categories={categories}/></div>}
